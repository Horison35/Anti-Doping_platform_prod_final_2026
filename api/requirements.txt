# api/requirements.txt — только то, что реально импортирует api/ в рантайме.
# Тяжёлые ML-зависимости (catboost, plotly, pdfplumber, joblib) сюда
# намеренно не входят — они нужны ml/predict.py и siar/*_report.py,
# которые исполняются на хосте (systemd), не в этом контейнере (см. DEPLOY.md).
fastapi>=0.115
uvicorn[standard]>=0.30
psycopg[binary]>=3.1
psycopg-pool>=3.2
itsdangerous>=2.2
python-dotenv>=1.0
pandas>=2.3
openpyxl>=3.1
pydantic>=2.12
